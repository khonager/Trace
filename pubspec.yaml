name: trace
description: "A privacy-first Matrix client with local-first extensions."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  app_links: 7.0.0
  cupertino_icons: ^1.0.8
  file_picker: ^12.1.2
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  flutter_vodozemac: ^0.7.1
  matrix: ^10.2.2
  path: ^1.9.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2
  sqflite_common_ffi: 2.4.0
  url_launcher: ^6.3.2
  vodozemac: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profiles/
